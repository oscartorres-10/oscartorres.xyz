---
const { postUrl, postTitle, postPubDate } = Astro.props
---

<li class='flex gap-3'>
  <span class='hidden md:inline'>{postPubDate.slice(0, 10)}</span>
  <a class='underline' href={postUrl}>{postTitle}</a>
</li>
